variable "project_name" {
  default = "myproject"       # ← change this (lowercase, no spaces)
}

variable "aws_region" {
  default = "ap-south-1"     # ← change if needed
}

variable "domain_name" {
  default = "yourdomain.com" # ← change to your domain
}